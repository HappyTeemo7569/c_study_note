/**********************
作用：请求编译器将局部变量存储于寄存器中，但是如果寄存器不答应（比如满了），则请求无效。
变化：C语言中不能取得register变量地址，C++可以，但是要取则使得该声明无效。
目的：如果变量需要被频繁使用，则保存在寄存器中可以加快存取速度。
注意：
	通常需要变量是单个的值，长度小于等于int型，不过有些机器也可以存放浮点数。
	&不能取寄存器地址。
	不是必然提高速度，寄存器被占用但是被使用的次数不够多，则划不来。
	是暗示不是命令。
***********************/